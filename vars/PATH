case ":$PATH:" in
  *":/opt/scripts:"*) ;;
  *) PATH="/opt/scripts:$PATH" ;;
esac

case ":$PATH:" in
  *":/opt/bin:"*) ;;
  *) PATH="/opt/bin:$PATH" ;;
esac

case ":$PATH:" in
  *":$HOME/.local/bin:"*) ;;
  *) PATH="$HOME/.local/bin:$PATH" ;;
esac

export PATH
